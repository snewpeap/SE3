spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        search:
          default:
            directory_provider: "filesystem"
            indexBase: "/var/lucene/indexes"

server:
  port: 9090

