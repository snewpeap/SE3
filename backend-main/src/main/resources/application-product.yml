spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        search:
          default:
            directory_provider: "filesystem"
            indexBase: "/var/lucene/indexes"

server:
  port: 9090

parse-csv:
  need: false
